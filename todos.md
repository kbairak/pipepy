- [x] When piping to function, inspect the function's signature and pass
      arguments accordingly
- [x] When piping to generator, return the return value of the generator (via
      `StopIteration`'s argument)
